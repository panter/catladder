{
  "name": "@catladder/bash",
  "description": "type-safe bash script generation primitives (BashExpression, escaping, variable values) — internal package, compiled from source into @catladder/cli",
  "private": true,
  "author": "Marco Wettstein <maw@panter.ch>",
  "license": "MIT",
  "version": "0.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix"
  },
  "devDependencies": {
    "@types/node": "^22.5.1",
    "typescript": "^5.4.5",
    "yaml": "^2.5.0"
  }
}
