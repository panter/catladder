{
  "name": "catladder",
  "version": "0.0.0",
  "author": "Marco Wettstein <maw@panter.ch>",
  "license": "MIT",
  "scripts": {
    "test": "yarn workspace @catladder/pipeline generate:examples-test && vitest run",
    "test:update": "yarn workspace @catladder/pipeline generate:examples-test && vitest run -u",
    "build": "yarn workspaces foreach --exclude='docs' -v --topological-dev run build",
    "lint": "yarn workspaces foreach run lint",
    "dev": "yarn workspaces foreach -vi --parallel run build:watch",
    "postinstall": "husky install",
    "prepublishOnly": "pinst --disable",
    "postpublish": "pinst --enable",
    "pretty": "prettier --write \"./**/*.{js,jsx,mjs,cjs,ts,tsx,json}\"",
    "catenv": "./cli/bin/catenv-dev",
    "catladder": "./cli/bin/catladder-dev"
  },
  "workspaces": [
    "pipeline",
    "cli",
    "docs"
  ],
  "devDependencies": {
    "@eslint/js": "^9.9.1",
    "@types/eslint__js": "^8.42.3",
    "@typescript-eslint/eslint-plugin": "^8.3.0",
    "@typescript-eslint/parser": "^8.3.0",
    "@vercel/ncc": "^0.38.1",
    "eslint": "9.x",
    "eslint-config-prettier": "^9.1.0",
    "eslint-formatter-unix": "^8.40.0",
    "eslint-plugin-prettier": "^5.2.1",
    "globals": "^15.11.0",
    "husky": ">=6",
    "lint-staged": ">=10",
    "pinst": ">=2",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5",
    "typescript-eslint": "^8.3.0",
    "vitest": "^2.1.8"
  },
  "packageManager": "yarn@3.1.1",
  "lint-staged": {
    "*.ts": [
      "eslint --cache --fix"
    ]
  }
}
