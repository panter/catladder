---
"catladder": minor
---

New deploy types: `npmPackage` publishes a component to npm on tagged releases (workspace-monorepo aware), and `pages` deploys static sites to GitLab Pages with per-environment previews. Custom deploys gained `artifactsPaths` and `allowFailure`.
