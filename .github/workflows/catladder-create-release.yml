# -------------------------------------------------
# 🐱 🔨 This file is generated by catladder
# 🚨 Do not edit this file manually 🚨
# -------------------------------------------------

name: 🚀 catladder create release
on:
  workflow_dispatch:
    inputs:
      force:
        description: 'force: release even without pending changes (patch bump), ignoring the pipeline state'
        required: false
        type: boolean
        default: false
permissions:
  contents: read
  packages: write
env:
  CL_JOB_TOKEN: ${{ github.token }}
  CL_REGISTRY: ghcr.io
  CL_REGISTRY_IMAGE: ghcr.io/${{ github.repository }}
  CL_REGISTRY_USER: ${{ github.actor }}
  CL_PR_NUMBER: ${{ github.event.number }}
jobs:
  catladder-image-changesets:
    name: 🐳 catladder image changesets
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: 🐳 catladder image changesets
        id: main
        run: |-
          function escapeForDotEnv () {
          input="${1:-$(cat)}"
           input="${input//$'\n'/\\n}"
            if [[ "$input" == *\\n* ]]; then
              if [[ "$input" == *\"* && "$input" == *\'* && "$input" == *\`* ]]; then
                printf "\"%s\"\n" "$input" 
              elif [[ "$input" == *\"* && "$input" == *\'* ]]; then
                printf "`%s`\n" "$input"
              elif [[ "$input" == *\"* ]]; then
                printf "'%s'\n" "$input"
              else
                printf "\"%s\"\n" "$input"
              fi
            else
              printf "%s\n" "$input"
            fi
          }
          function collapseable_section_start () {
          local section_title="${1}"
            local section_description="${2:-$section_title}"
            echo "::group::${section_description}"
          }
          function collapseable_section_end () {
          echo "::endgroup::"
          }
          collapseable_section_start "docker-login" "Docker Login"
          docker login --username $CL_REGISTRY_USER --password $CL_JOB_TOKEN $CL_REGISTRY
          collapseable_section_end "docker-login"
          collapseable_section_start "check-image" "Checking if image exists"
          if docker manifest inspect ghcr.io/${{ github.repository }}/catladder/changesets:d9899ef59230 > /dev/null 2>&1; then
            echo "Image ghcr.io/${{ github.repository }}/catladder/changesets:d9899ef59230 already exists, skipping build"
            exit 0
          fi
          collapseable_section_end "check-image"
          collapseable_section_start "docker-build" "Building image"
          DOCKER_BUILDKIT=1 docker build -t ghcr.io/${{ github.repository }}/catladder/changesets:d9899ef59230 -f .catladder-generated/images/changesets/Dockerfile .catladder-generated/images/changesets
          collapseable_section_end "docker-build"
          collapseable_section_start "docker-push" "Pushing image"
          docker push ghcr.io/${{ github.repository }}/catladder/changesets:d9899ef59230
          collapseable_section_end "docker-push"
        shell: bash
  create-release:
    name: create release
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository }}/catladder/changesets:d9899ef59230
      credentials:
        username: ${{ github.actor }}
        password: ${{ github.token }}
    permissions:
      contents: write
      actions: write
      packages: read
    needs:
      - catladder-image-changesets
    env:
      GITHUB_TOKEN: ${{ github.token }}
      CATLADDER_FORCE_RELEASE: ${{ inputs.force && 'true' || '' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: check main workflow
        id: queue
        if: ${{ !inputs.force }}
        run: node .catladder-generated/catci/index.js release github-queue-check
        shell: bash
      - name: create release
        if: steps.queue.outputs.queued != 'true'
        run: changesetsRelease
        shell: bash
